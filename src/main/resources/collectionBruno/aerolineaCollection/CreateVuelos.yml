info:
  name: CreateVuelos
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/vuelos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
