info:
  name: CreatePasajeros
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/pasajeros
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
