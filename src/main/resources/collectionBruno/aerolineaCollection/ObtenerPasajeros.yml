info:
  name: ObtenerPasajeros
  type: http
  seq: 1

http:
  method: GET
  url: localhost:8080/pasajeros
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
