info:
  name: CreatePasajeros
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/pasajeros
  body:
    type: json
    data: |-
      {
        "nombre":"maira",
        "apellido":"Pinilla",
        "documento":"1122213",
        "email":"email@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
